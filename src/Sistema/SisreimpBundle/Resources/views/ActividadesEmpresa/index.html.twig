{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ActividadesEmpresa list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Idempresa</th>
                <th>Idactividad</th>
                <th>Fechainicio</th>
                <th>Status</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('actividadesempresa_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.idEmpresa }}</td>
                <td>{{ entity.idActividad }}</td>
                <td>{% if entity.fechaInicio %}{{ entity.fechaInicio|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.status }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('actividadesempresa_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('actividadesempresa_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('actividadesempresa_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
