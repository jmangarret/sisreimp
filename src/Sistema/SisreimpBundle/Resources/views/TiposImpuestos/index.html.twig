{% extends '::base.html.twig' %}

{% block body -%}
    <h1>TiposImpuestos list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Idrubrotributario</th>
                <th>Nombre</th>
                <th>Tipo</th>
                <th>Valoraplicable</th>
                <th>Status</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('tiposimpuestos_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.idRubroTributario }}</td>
                <td>{{ entity.nombre }}</td>
                <td>{{ entity.tipo }}</td>
                <td>{{ entity.valorAplicable }}</td>
                <td>{{ entity.status }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('tiposimpuestos_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('tiposimpuestos_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('tiposimpuestos_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
