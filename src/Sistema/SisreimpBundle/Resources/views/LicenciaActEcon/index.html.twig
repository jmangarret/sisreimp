{% extends '::base.html.twig' %}

{% block body -%}
    <h1>LicenciaActEcon list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Idempresa</th>
                <th>Fecha</th>
                <th>Vigenciadesde</th>
                <th>Vigenciahasta</th>
                <th>Status</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('licenciaactecon_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.idEmpresa }}</td>
                <td>{% if entity.fecha %}{{ entity.fecha|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.vigenciaDesde %}{{ entity.vigenciaDesde|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.vigenciaHasta %}{{ entity.vigenciaHasta|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.status }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('licenciaactecon_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('licenciaactecon_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('licenciaactecon_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
