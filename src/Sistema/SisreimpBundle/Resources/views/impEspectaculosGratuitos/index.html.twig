{% extends '::base.html.twig' %}

{% block body -%}
    <h1>impEspectaculosGratuitos list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombretipoespectaculo</th>
                <th>Valorut</th>
                <th>Status</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('impespectaculosgratuitos_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombreTipoEspectaculo }}</td>
                <td>{{ entity.valorUt }}</td>
                <td>{{ entity.status }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('impespectaculosgratuitos_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('impespectaculosgratuitos_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('impespectaculosgratuitos_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
