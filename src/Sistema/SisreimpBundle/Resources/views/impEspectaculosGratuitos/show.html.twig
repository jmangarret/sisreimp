{% extends '::base.html.twig' %}

{% block body -%}
    <h1>impEspectaculosGratuitos</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nombretipoespectaculo</th>
                <td>{{ entity.nombreTipoEspectaculo }}</td>
            </tr>
            <tr>
                <th>Valorut</th>
                <td>{{ entity.valorUt }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ entity.status }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('impespectaculosgratuitos') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('impespectaculosgratuitos_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
