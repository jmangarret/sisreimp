{% extends '::base.html.twig' %}

{% block body -%}
    <h1>impCobroEntradas list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Cantdesde</th>
                <th>Canthasta</th>
                <th>Totalut</th>
                <th>Porcentajemontoentradas</th>
                <th>Status</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('impcobroentradas_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.cantDesde }}</td>
                <td>{{ entity.cantHasta }}</td>
                <td>{{ entity.totalUt }}</td>
                <td>{{ entity.porcentajeMontoEntradas }}</td>
                <td>{{ entity.status }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('impcobroentradas_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('impcobroentradas_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('impcobroentradas_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
