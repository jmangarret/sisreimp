{% extends '::base.html.twig' %}

{% block body -%}
    <h1>tipoVehiculo</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Tipo</th>
                <td>{{ entity.tipo }}</td>
            </tr>
            <tr>
                <th>Montoimpuesto</th>
                <td>{{ entity.montoImpuesto }}</td>
            </tr>
            <tr>
                <th>Porcalicuota</th>
                <td>{{ entity.porcAlicuota }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ entity.status }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('tipovehiculo') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('tipovehiculo_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
